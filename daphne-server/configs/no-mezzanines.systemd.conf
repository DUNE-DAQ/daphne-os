# Optional board-local drop-in; install only when the operator confirms none fitted.
# Does not change network identity, clocks, BIASCTRL or generator-enable policy.
[Service]
Environment=DAPHNE_NO_MEZZANINES=1
